
// This component is no longer used as the inventory list has been converted to a table.
// It can be safely deleted from the project.
// For now, it's left empty to avoid build errors if it's accidentally imported elsewhere,
// though it should be removed.
